repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict

# Temporarily disabled until users app migrations are created
#  - repo: local
#    hooks:
#      - id: pytest-fast
#        name: pytest-fast
#        entry: .venv/Scripts/pytest.exe -m "not slow" --tb=short
#        language: system
#        pass_filenames: false
#        always_run: true
