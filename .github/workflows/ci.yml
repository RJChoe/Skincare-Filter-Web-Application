name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

# Cancel superseded runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  PYTHONUNBUFFERED: "1"
  DJANGO_SETTINGS_MODULE: "skincare_project.settings"

jobs:
  # ============================================================================
  # Lint Job - Guards the test job
  # ============================================================================
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          uv sync --extra dev

      - name: Run pre-commit (ruff, ruff-format, mypy)
        run: |
          uv run pre-commit run --all-files

  # ============================================================================
  # Test Job - Depends on successful static analysis
  # ============================================================================
  test:
    name: Test (Python 3.14)
    runs-on: ubuntu-latest
    needs: static-analysis
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          uv sync --extra dev

      - name: Run migrations
        run: |
          uv run python manage.py migrate --noinput

      - name: Run tests with coverage
        run: |
          uv run pytest

      - name: Upload coverage to Codecov
        if: github.event.pull_request.head.repo.fork == false
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

  # ============================================================================
  # Static Analysis Job - Guards the test job
  # ============================================================================
  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    needs: lint
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: |
          uv sync --extra dev

      - name: Run bandit
        run: |
          uv run bandit -r . --severity-level high --confidence-level high -x htmlcov,venv,.venv -f json -o bandit-output.json

      - name: Upload bandit report
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit-output.json

      - name: Run safety
        run: |
          uv run safety check --full-report --output-format=json > safety-output.json || true

      - name: Upload safety report
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: safety-report
          path: safety-output.json

  # ============================================================================
  # Aggregate CI status for branch protection
  # ============================================================================
  ci-success:
    name: ci-success
    runs-on: ubuntu-latest
    needs: [lint, static-analysis, test]
    if: ${{ always() }}

    steps:
      - name: Write CI Summary Report
        if: ${{ always() }}
        run: |
          map_status() {
            case "$1" in
              success)
                echo "✅ Pass"
                ;;
              failure)
                echo "❌ Fail"
                ;;
              cancelled)
                echo "❌ Cancelled"
                ;;
              *)
                echo "⚠️ Warning"
                ;;
            esac
          }

          echo "### CI Status Report 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "| :--- | :--- |" >> $GITHUB_STEP_SUMMARY

          LINT_STATUS=$(map_status "${{ needs.lint.result }}")
          STATIC_ANALYSIS_STATUS=$(map_status "${{ needs['static-analysis'].result }}")
          TEST_STATUS=$(map_status "${{ needs.test.result }}")

          echo "| Lint | $LINT_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "| Static Analysis | $STATIC_ANALYSIS_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "| Test (Matrix) | $TEST_STATUS |" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- [Bandit Security Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts)" >> $GITHUB_STEP_SUMMARY
          echo "- [Safety Vulnerability Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts)" >> $GITHUB_STEP_SUMMARY

      - name: Enforce aggregate status
        run: |
          if [ "${{ needs.lint.result }}" = "success" ] && [ "${{ needs['static-analysis'].result }}" = "success" ] && [ "${{ needs.test.result }}" = "success" ]; then
            echo "All required jobs succeeded."
            exit 0
          else
            echo "One or more required jobs failed or were cancelled."
            exit 1
          fi
